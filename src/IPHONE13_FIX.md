# 📱 Исправление для iPhone 13

## ✅ Что изменено:

### **1. Убран клик на SwipeUpIndicator**
- ❌ Больше **НЕТ** клика на индикатор внизу
- ✅ Теперь **ТОЛЬКО свайп** снизу вверх открывает Bottom Sheet
- ✅ Клавиатурные события не мешают

### **2. Увеличены отступы для iPhone 13**
**Было:** `max(4rem, calc(env(safe-area-inset-bottom) + 1rem))` = ~64px  
**Стало:** `max(10rem, calc(env(safe-area-inset-bottom) + 8rem))` = ~160px

Это учитывает:
- Safe area bottom: ~34px (home indicator)
- SwipeUpIndicator: ~80px (уменьшен до 80px с учётом safe area)
- Меню Safari: ~50px
- **Итого зазор:** 160px+ для полного доступа к кнопкам

### **3. Уменьшен SwipeUpIndicator**
**Было:** фиксированная высота `h-20` (80px)  
**Стало:** динамическая высота `max(5rem, calc(env(safe-area-inset-bottom) + 3rem))`

Теперь индикатор адаптируется под safe area iPhone 13.

---

## 🎯 Проверка на iPhone 13:

### **Экран Orders:**
1. Прокрутите список из 10 ордеров вниз до конца
2. **Кнопки должны быть ПОЛНОСТЬЮ видны:**
   - ✅ "Confirm Market" (зелёная)
   - ✅ "Open Limit" (синяя)
   - ✅ "Cancel Limit" (красная)
3. **Ещё ниже:**
   - ✅ "Rollover" (серая) → нажмите → появится уведомление "✅ Rollover успешно выполнен!"
   - ✅ "Stop" (серая) → нажмите → появится уведомление "🛑 Stop активирован!"
4. **Отступ внизу:** ~160px от последних кнопок до края экрана

### **Проверка кнопок Rollover и Stop:**
Если вы видите всплывающие уведомления (toast) при нажатии на эти кнопки - значит они **НЕ перекрыты** меню Safari и полностью кликабельны! 🎉

- **Rollover** → зелёное уведомление "Все позиции продлены на следующий период"
- **Stop** → красное уведомление "Все активные ордера остановлены"

### **SwipeUpIndicator:**
- Свайп снизу вверх (быстрый или >50px) → открывается Bottom Sheet ✅
- **Клик НЕ работает** → не мешает клавиатуре ✅
- Tap вне Bottom Sheet → закрывается ✅

### **Вертикальный скролл:**
- Menu → раскройте "Crypto" (7 монет) → скролл работает ✅
- Orders → скролл 10 ордеров → доступ до кнопок ✅
- Finance → скролл 4 элемента ✅

### **Горизонтальный свайп:**
- Finance ↔️ Menu ↔️ Orders
- Работает только при явном горизонтальном свайпе

---

## 📐 Размеры iPhone 13:

```
Экран: 6.1 дюйма (1170 x 2532 px)
Safe area top: ~47px (Dynamic Island)
Safe area bottom: ~34px (Home Indicator)
Safari menu (верх): ~44px
Safari menu (низ): ~49px при скролле
```

---

## 🔧 Если кнопки всё ещё перекрыты:

### **Вариант 1: Добавить на домашний экран (РЕКОМЕНДУЕТСЯ)**
1. Safari → кнопка "Поделиться" ⬆️
2. "На экран «Домой»"
3. "Добавить"
4. Открыть с домашнего экрана → **БЕЗ меню Safari!**

### **Вариант 2: Увеличить отступ вручную**
Если кнопки всё ещё недоступны, откройте `/components/OrdersScreen.tsx` и измените строку 287:

```javascript
// Было:
marginBottom: 'max(10rem, calc(env(safe-area-inset-bottom) + 8rem))'

// Попробуйте:
marginBottom: 'max(12rem, calc(env(safe-area-inset-bottom) + 10rem))'
```

Это даст ещё +32px отступа.

---

## 🎨 Визуальная проверка:

### **Правильно (после исправлений):**
```
┌──────────────────────────┐
│   [Safari URL bar]       │
├──────────────────────────┤
│   [Orders List]          │
│   [Scrollable]           │
│                          │
│   ✅ [Confirm Market]    │ ← Полностью видно
│   ✅ [Open Limit]        │ ← Полностью видно
│   ✅ [Cancel Limit]      │ ← Полностью видно
│                          │
│   ✅ [Rollover] [Stop]   │ ← Полностью видно
│                          │
│   [~160px отступ]        │ ← Большой зазор
│                          │
│   [ChevronUp ⬆️]         │ ← SwipeUpIndicator
├──────────────────────────┤
│   [Safari Menu]          │ ← Не перекрывает кнопки!
└──────────────────────────┘
     [Home Indicator]
```

### **Неправильно (было раньше):**
```
┌──────────────────────────┐
│   [Safari URL bar]       │
├──────────────────────────┤
│   [Orders List]          │
│                          │
│   ❌ [Confirm Market]    │ ← Частично скрыто
│   ❌ [Open Limit]        │ ← Частично скрыто
│   ❌ [Cancel Limit]      │ ← Скрыто полностью
├──────────────────────────┤
│   [Safari Menu]          │ ← Перекрывает!
└──────────────────────────┘
```

---

## 💡 Дополнительно:

### **Отключить SwipeUpIndicator (если не нужен Bottom Sheet):**
В `/App.tsx` закомментируйте строку 185:
```typescript
{/* <SwipeUpIndicator onSwipeUp={() => setIsBottomSheetOpen(true)} /> */}
```

Это освободит ещё ~80px внизу экрана.

---

## 🐛 Отладка:

### **Проблема:** Кнопки всё ещё не кликаются
**Причина:** SwipeUpIndicator с `z-30` перекрывает их  
**Решение:** Уменьшите z-index в `/components/SwipeUpIndicator.tsx`:
```typescript
// Было: z-30
// Попробуйте: z-10
className="fixed bottom-0 left-0 right-0 ... z-10 pointer-events-none"
```

### **Проблема:** Bottom Sheet не открывается свайпом
**Причина:** SwipeUpIndicator слишком маленький  
**Решение:** Увеличьте touch area в `/components/SwipeUpIndicator.tsx`:
```typescript
<div 
  className="pointer-events-auto py-6" // было py-4
  onTouchStart={handleTouchStart}
  onTouchEnd={handleTouchEnd}
>
```

---

**Дата:** 9 декабря 2024  
**Устройство:** iPhone 13 (6.1", iOS 16+)  
**Браузер:** Safari  
**Статус:** ✅ Исправлено